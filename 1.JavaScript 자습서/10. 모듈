* 모듈 *
  - 일반적으로 파일 경로 또는 URL로 식별되는 파일
  - import 및 export로 모듈 간에 데이터를 교환하는 명령문으로 사용 가능
    import { foo } from "./foo.js"; // foo.js파일에 있는 foo를 가져온다

    // export 하지 않은 변수는 모듈 내에서 지역 변수 범위를 가집니다.
    const b = 2;

    export const a = 1; // 다른 파일에서 사용 가능하게끔 const a = 1;를 공유?한다